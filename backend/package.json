{
  "name": "@k8s-monitor/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.log('backend workspace scaffold ready; Fastify implementation pending')\"",
    "build": "node -e \"console.log('backend build pending implementation')\"",
    "test": "node -e \"console.log('backend tests pending implementation')\"",
    "lint": "node -e \"console.log('backend lint pending implementation')\""
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.3.0",
    "fastify": "^5.3.2"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  }
}
